rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Expenses collection
    match /expenses/{expenseId} {
      // Allow anyone to read and write expenses
      // In production, you should add authentication rules
      allow read, write: if true;
    }
  }
}
